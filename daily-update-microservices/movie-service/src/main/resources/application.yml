server:
  port: ${HTTP_PORT:10010}
theMovieDb:
  ribbon:
    listOfServers: ${themoviedb.baseUrl}
logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
  file: ${LOG_LOCATION:/var/log/daily-update/movie-service.log}
