server:
  port: ${HTTP_PORT:10030}
nyt:
  ribbon:
    listOfServers: ${openWeatherMap.baseUrl}
logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG
  file: ${LOG_LOCATION:/var/log/daily-update/${spring.application.name:weather-service}.log}
